S08 — BoringSSL gap (notes)

This session is a short exploration of API differences between OpenSSL and BoringSSL. It contains notes and pointers. Local execution depends on system libraries and is environment-specific.

See docs/overview.md for further reading and references to OpenSSL ENGINE vs PROVIDER.