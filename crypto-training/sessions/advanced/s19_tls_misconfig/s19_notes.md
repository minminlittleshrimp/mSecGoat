S19 — TLS misconfiguration

Exercises:
- Use `s05_tls_inspect` to create insecure server configs (weak ciphers, disabled verify) and observe that connections still work.
- Use `testssl.sh` or `openssl s_client` to enumerate ciphers and check verification settings.
