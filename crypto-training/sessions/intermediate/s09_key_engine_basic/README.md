S09 — Key Engine (Basic)

Start the engine and exercise sign/mac via key_id.

Run:
  # start engine in background
  python3 ../engine/engine_server.py &

  # then from this dir
  python3 demo.py

This demo shows no key export and key usage by id only.