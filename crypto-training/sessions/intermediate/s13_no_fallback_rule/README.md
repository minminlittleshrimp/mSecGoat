S13 — No-fallback rule

Broken wrapper falls back to local crypto on engine failure causing silent bypass. Compare wrapper/crypto_wrapper.py (safe) vs wrapper/buggy_wrapper.py (broken).

Run demo to see the difference.