Notes:
- Each session has a README and a runnable demo script
- Run labs/run_all.py to execute a curated set of demos (requires a working venv and dependencies)
- Engine must be started as a separate process to enforce boundary semantics
