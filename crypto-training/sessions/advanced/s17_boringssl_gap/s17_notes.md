S17 — BoringSSL gap

This session documents common API differences and how to test equivalence.

- Try to build a minimal example against OpenSSL and BoringSSL and observe missing symbols.
- Use vendor docs to decide compatibility.
