S16 — OpenSSL ENGINE vs PROVIDER

Notes and commands (environment-dependent). This session is documentation-heavy: it explains how legacy ENGINEs are loaded and how the PROVIDER model replaces that in OpenSSL 3.x.

See docs/overview.md for links. Hands-on requires system OpenSSL and compiled engines which are out-of-scope for this pure-Python demo.