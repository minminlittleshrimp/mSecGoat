S20 — Failure Lab

Combine multiple failures into a single scenario:
- Nonce reuse (AES-GCM)
- Weak RNG for key generation
- Vulnerable fallback enabled
- TLS verification disabled

Goal: show that a system can "work" but be insecure; run each broken-case demo and write a short report with findings.
